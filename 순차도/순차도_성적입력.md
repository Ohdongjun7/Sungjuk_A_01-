```mermaid

sequenceDiagram
    autonumber
    actor 교수
    participant UI as Add_Sunghk_UI <<boundary>>
    participant T as 교수1:교수
    participant G as 성적1:성적

    교수->>UI: 교수id, 학생id, 성적(자바, DB, 보안) 입력
    UI->>T: 교수체크(교수id)
    
    alt 교수id == 'inha'
        T-->>UI: 체크 성공
        UI->>G: 성적입력(학생id, 자바, DB, 보안)
        activate G
        Note over G: 총점 및 평균 계산
        G-->>UI: 입력 완료
        deactivate G
        
        UI->>G: 학점조회(학생id)
        activate G
        Note over G: 평균 기준 학점 부여<br/>(A: 90↑, B: 80↑, C: 80↓)
        G-->>UI: 학점 결과 반환
        deactivate G
        
        UI-->>교수: 성적 등록 및 학점 산출 결과 표시
    else 교수id != 'inha'
        T-->>UI: 체크 실패 (권한 없음)
        UI-->>교수: "교수 ID를 확인하세요" 메시지 출력
    end